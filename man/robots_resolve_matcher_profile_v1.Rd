% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve-matcher-profile-v1.R
\name{robots_resolve_matcher_profile_v1}
\alias{robots_resolve_matcher_profile_v1}
\title{Resolve a matcher-profile selector before evaluating anything}
\usage{
robots_resolve_matcher_profile_v1(matcher_backend, robots_product_token)
}
\arguments{
\item{matcher_backend}{A character vector of matcher backends, length one or
\code{length(robots_product_token)}: \code{"google"}, \code{"yandex"}, \code{"rfc9309"}, or
\code{"bing"}.}

\item{robots_product_token}{A character vector of selectors, length one or
\code{length(matcher_backend)}.}
}
\value{
A data frame with one row per resolved selector and the columns
\code{matcher_backend}, \code{robots_product_token} (the value as supplied),
\code{token_policy}, \code{profile_id}, \code{accepted_token} (the canonical spelling),
\code{group_label}, \code{group_selection}, and \code{profile_revision}. For a backend
whose \code{token_policy} is not \code{"bounded_profiles"} there is no closed set to
resolve against, so the profile columns are \code{NA} and the token is returned
unchanged.

\code{group_selection} is \code{"exact_else_wildcard"} when the profile falls back to
\verb{User-agent: *} groups if no group names it, and \code{"exact_only"} when it
never does. Two accepted selectors on one backend are therefore not
interchangeable.
}
\description{
Checks, at configuration-construction time, that a product token is one the
named matcher backend will actually accept — and reports what it will DO,
not merely that it is accepted. Performs no fetch and no matching.
}
\details{
A \code{bounded_profiles} backend (\code{"yandex"}, \code{"bing"}) accepts a closed set of
selectors. Passing an unaccepted one is not an error during evaluation: the
affected rows come back undecided, with \code{url_decision} \code{NA}, which reads like
a transport failure rather than a configuration mistake. This function turns
that quiet outcome into a loud one at the point where the mistake is
actually made.

Checking the evaluation results instead is not equivalent. Profile support
is a property of the matcher, and a row need not reach the matcher at all —
an \code{allow_all} policy returns a decision for a nonsensical selector without
ever consulting the profile.

The \code{robots_product_token} is a robots.txt \verb{User-agent:} group selector, not
an HTTP \code{User-Agent} crawler identity. The distinction is easy to miss
because RFC 9309 treats them as one namespace, and vendors do not: Yandex's
\code{"Yandex"} is an umbrella group label that no crawler ever sends, while
\code{"YandexBot"} — a real crawler — is not an accepted selector at all.
}
\examples{
# The umbrella group label and the Additional crawler are both accepted, but
# they select groups differently.
robots_resolve_matcher_profile_v1(
  "yandex", c("Yandex", "YandexAdditionalBot")
)

# Selectors are compared ASCII case-insensitively; the canonical spelling
# comes back in `accepted_token`.
robots_resolve_matcher_profile_v1("bing", "BINGBOT")

# A real crawler identity is not a selector. This is the mistake the
# function exists to catch, so it errors rather than returning a row.
try(robots_resolve_matcher_profile_v1("yandex", "YandexBot"))

# An unbounded backend has no closed set to resolve against.
robots_resolve_matcher_profile_v1("google", "Googlebot")
}
\seealso{
\code{\link[=robots_engine_contract_v1]{robots_engine_contract_v1()}}, whose \code{matcher_capability} publishes
the same data as a table.
}
