% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robots_body.R
\name{robots_body}
\alias{robots_body}
\title{Preview or extract a stored robots.txt body}
\usage{
robots_body(x, source_id = NULL, n = 20, raw = FALSE)
}
\arguments{
\item{x}{A \code{robots_decisions} object.}

\item{source_id}{A single non-empty character string naming the source to
read from \code{x$robots$source_id}, or \code{NULL} (the default) to select the only
source when the object holds exactly one.}

\item{n}{A single positive number giving the maximum number of body bytes to
include in the preview. Defaults to \code{20}. Use \code{n = Inf} for the full body.
Ignored when \code{raw = TRUE}.}

\item{raw}{A single logical. When \code{TRUE}, return the stored raw vector
unchanged (byte-for-byte identical to \code{x$robots$body[[i]]}) instead of a
rendered preview. Defaults to \code{FALSE}.}
}
\value{
When \code{raw = FALSE}, a length-one character string (a preview, or the
full body when \code{n = Inf}), marked \code{Encoding = "UTF-8"} or \code{Encoding = "bytes"}. When \code{raw = TRUE}, the stored raw vector unchanged (or \code{NULL}
when the selected source stored no body).
}
\description{
Returns the robots.txt body stored in a \code{robots_decisions} object. Each
source body is stored once as a raw vector in the object's \code{robots} table;
this helper selects one source and either renders a character preview
(\code{raw = FALSE}, the default) or returns the stored raw vector byte-for-byte
unchanged (\code{raw = TRUE}).
}
\details{
Selection follows the \code{robots$source_id} column. If \code{source_id} is \code{NULL}
(the default) and the object holds exactly one source, that source is used.
If \code{source_id} is \code{NULL} and the object holds more than one source, the call
is
ambiguous and raises a package error rather than choosing silently. An
unknown \code{source_id} is also a package error.

When \code{raw = FALSE}, the selected body's bytes are rendered to a character
string. Robots.txt bodies may be arbitrary bytes (for example non-UTF-8 or a
value carrying \code{Encoding = "bytes"}); rendering never errors on invalid
encoding. A body whose bytes form valid UTF-8 is returned as a UTF-8 string;
otherwise the result carries \code{Encoding = "bytes"} so R does not attempt to
reinterpret it. \code{n} limits the preview to the first \code{n} bytes of the body;
\code{n = Inf} returns the full body. \code{n} is ignored when \code{raw = TRUE}, which
always returns the complete stored raw vector unchanged.
}
\examples{
x <- allowed_by_robots_text("user-agent: *\ndisallow: /x", "http://x/x", "b")
robots_body(x)
robots_body(x, n = Inf)
robots_body(x, raw = TRUE)

}
