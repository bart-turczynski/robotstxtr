% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robots_validate.R
\name{robots_validate_text}
\alias{robots_validate_text}
\title{Validate a supplied robots.txt document}
\usage{
robots_validate_text(robots_txt, source_id = "supplied")
}
\arguments{
\item{robots_txt}{A single non-missing character string or a raw vector.}

\item{source_id}{A single non-empty character string identifying the supplied
document. Defaults to \code{"supplied"}.}
}
\value{
An S3 object of class \code{robots_validations}, containing \code{map},
\code{documents}, and \code{diagnostics} data frames.
}
\description{
Parses a supplied document with the package's pinned Google-compatible
parser and returns byte evidence, directive counts, and stable per-line
diagnostics. This validates document syntax and structure; it does not
decide whether a URL is allowed, infer webmaster intent, or fetch sitemap
resources declared by the document.
}
\details{
The validation profile is explicitly \code{"google-parser-compatible"}. It
reports what the vendored parser recognizes (including the parser's accepted
typo and missing-colon variants) and adds conservative structural checks. It
is not a claim of universal crawler behavior or complete RFC 9309 validity.

\code{robots_txt} may be text or a raw vector. Raw input is useful when validating
acquisition bytes that contain malformed UTF-8, a byte-order mark, or NUL
bytes. Character input marked with \code{Encoding = "bytes"} is used verbatim;
other character input is converted to UTF-8 once.

The result contains three typed data frames: \code{map} (the validation target),
\code{documents} (one summary row per document), and \code{diagnostics} (zero or more
findings). Diagnostic severities are \code{"info"}, \code{"warning"}, and \code{"error"};
document statuses are \code{"valid"}, \code{"warning"}, \code{"error"}, and
\code{"not_validated"}.
}
\examples{
report <- robots_validate_text(
  "user-agent: *\ndisallow: /private\n"
)
report$documents
report$diagnostics

malformed <- robots_validate_text(charToRaw(
  "disallow: /before-agent\nunknown-field: value"
))
malformed$diagnostics

}
\seealso{
\code{\link[=robots_validate_url]{robots_validate_url()}} to fetch and validate, \code{\link[=robots_fetch]{robots_fetch()}} for
acquisition evidence, and \code{\link[=allowed_by_robots_text]{allowed_by_robots_text()}} for URL decisions.
}
