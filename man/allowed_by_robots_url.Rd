% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allowed_by_robots_url.R
\name{allowed_by_robots_url}
\alias{allowed_by_robots_url}
\title{Fetch robots.txt and match URLs under the deterministic fetch policy}
\usage{
allowed_by_robots_url(
  url,
  user_agent,
  timeout = 10,
  max_bytes = 524288L,
  fetch_user_agent = NULL
)
}
\arguments{
\item{url}{A character vector of target URLs. May have length zero (an empty
result is returned). The original string is passed to the matcher
unchanged. A wrong R type is a call-level error.}

\item{user_agent}{A matcher user-agent string, length one or \code{length(url)},
passed to the matcher unchanged (upstream extraction semantics apply, so
e.g. \code{"Googlebot/2.1"} is not pre-trimmed by R).}

\item{timeout}{A single positive, finite numeric total timeout in seconds per
origin, including redirects. Defaults to \code{10}.}

\item{max_bytes}{A single positive, finite, whole-number decoded-body byte
limit no greater than \code{.Machine$integer.max}, coerced to integer. A
whole-number double (for example \code{524288}) is accepted; fractional,
non-positive, out-of-range, or non-scalar values are call-level errors.
Defaults to \code{524288L}.}

\item{fetch_user_agent}{\code{NULL} (use the package fetch user agent) or a single
non-empty character HTTP user agent to send instead. This is never the
matcher \code{user_agent}.}
}
\value{
An S3 object of class \code{robots_decisions}: a named list with two
components, \code{results} (one row per input URL, in input order) and \code{robots}
(one row per fetched source body). Each fetched body is stored once as a
raw vector in \code{robots$body}. Large body values never appear in \code{results}.
}
\description{
Fetches the governing \verb{/robots.txt} for one or more target URLs and matches
each URL against the fetched body using the vendored, upstream-test-validated
Google robots.txt matcher. This is the URL-first headline path: it combines
the grouped fetch stage (\code{robots_fetch()}) with matching and rich
per-row decisions, returning a \code{robots_decisions} object.
}
\details{
Each URL's fetch-origin grouping key \verb{scheme://host[:port]/robots.txt} is
constructed (see the fetch-origin rules in the package PRD). Within one call
each distinct grouping key is fetched exactly once and every fetch-eligible
row sharing that key reuses the same source body; source IDs are assigned
\code{robots_1}, \code{robots_2}, and so on in first-occurrence order. There is no
persistent, cross-call, or hidden HTTP cache.

The original input URL string is passed to the matcher unchanged. R does not
clean, decode, canonicalize, or reserialize it: percent escapes, Unicode,
path case, query strings, and fragments all reach the matcher as supplied.
\code{rurl} is used only to construct the fetch-origin grouping key, never to
transform the matcher input. The upstream matcher requires an appropriately
encoded, \verb{\%}-escaped full URL (per RFC 3986); callers are responsible for
supplying it in that form.

\code{url} may have length zero (an empty result is returned). \code{user_agent} must
be a character vector of length one or \code{length(url)}: a scalar user agent
expands across every URL, and no other recycling is allowed. A wrong R type,
a \code{user_agent} length mismatch, or an invalid \code{timeout}, \code{max_bytes}, or
\code{fetch_user_agent} are call-level errors.

A missing (\code{NA}), empty (\code{""}), malformed, or non-HTTP(S) URL, or a missing
or empty user-agent element, does not abort the call: that row yields a
per-row \code{input_unknown} decision with \code{allowed = NA}. URL validity is checked
before user-agent validity, so each invalid row has one deterministic primary
error class. An invalid row is fully detached: it never causes an HTTP
request and its \code{source_id}, \code{robots_url}, and \code{http_status} are \code{NA} with
\code{fetch_outcome = "input_invalid"}. A row with a valid URL but invalid user
agent stays detached even when a valid sibling row causes the same origin to
be fetched.

Per-row decisions follow the fetch outcome: a \code{200}-class body is matched
(\code{rule_disallow}/\code{rule_allow}/\code{default_allow}; an empty body is valid and
yields \code{default_allow}); a \code{404}/\code{410} yields \code{missing_allow}/\code{TRUE}; any
fetch failure yields \code{fetch_unknown}/\code{NA}. For matched rows, \code{matched_line},
\code{matched_rule_type}, and \code{matched_rule_value} are correlated from the parse
callbacks of the same source body (the canonical value the matcher used,
after upstream \code{MaybeEscapePattern} canonicalization).
}
\examples{
# allowed_by_robots_url() fetches /robots.txt over HTTP and matches in one
# step. The transport is mocked here so the example runs offline; a real call
# needs no such wrapper.
httr2::with_mocked_responses(
  function(req) {
    httr2::response(
      status_code = 200L, url = req$url,
      body = charToRaw("user-agent: *\nDisallow: /private\n")
    )
  },
  allowed_by_robots_url(
    c("https://example.com/page", "https://example.com/private"),
    "my-bot"
  )
)

}
\seealso{
\code{\link[=allowed_by_robots_text]{allowed_by_robots_text()}} to match a supplied body without HTTP,
and \code{\link[=robots_fetch]{robots_fetch()}} for the fetch stage on its own.
}
