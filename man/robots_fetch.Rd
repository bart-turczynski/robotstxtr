% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robots_fetch.R
\name{robots_fetch}
\alias{robots_fetch}
\title{Fetch robots.txt documents for URLs under the deterministic fetch policy}
\usage{
robots_fetch(url, timeout = 10, max_bytes = 524288L, fetch_user_agent = NULL)
}
\arguments{
\item{url}{A character vector of target URLs. May have length zero (an empty
result is returned). A wrong R type is a call-level error.}

\item{timeout}{A single positive, finite numeric total timeout in seconds per
origin, including redirects. Defaults to \code{10}.}

\item{max_bytes}{A single positive, finite, whole-number decoded-body byte
limit no greater than \code{.Machine$integer.max}, coerced to integer and
recorded on each source row. A whole-number double (for example \code{524288})
is accepted; fractional, non-positive, out-of-range, or non-scalar values
are call-level errors. Defaults to \code{524288L}.}

\item{fetch_user_agent}{\code{NULL} (use the package fetch user agent) or a single
non-empty character HTTP user agent to send instead.}
}
\value{
An S3 object of class \code{robots_fetches}: a named list with two
components, \code{map} (one row per input URL, in input order) and \code{robots} (one
row per fetched source, with the fetched body stored once as a raw vector
in \code{robots$body}).
}
\description{
Performs the grouped robots.txt fetch stage for one or more target URLs and
returns a \code{robots_fetches} object. No matching is performed (that is the
\code{allowed_by_robots_url()} path, a later slice); this function only computes
each URL's
fetch-origin, fetches each distinct \verb{/robots.txt} once, and classifies the
HTTP outcome.
}
\details{
For every input URL the fetch-origin grouping key
\verb{scheme://host[:port]/robots.txt} is constructed (see the fetch-origin rules
in the package PRD). Within one call each distinct grouping key is fetched
exactly once and every input row sharing that key references the same source
row and stored body. Grouping is keyed on the requested robots URL, not on
the final redirect destination. Input order is preserved in \code{map}, and fetch
groups run sequentially in first-occurrence order; source IDs are assigned
\code{robots_1}, \code{robots_2}, and so on in that order. There is no persistent,
cross-call, or hidden HTTP cache.

A URL whose fetch-origin cannot be constructed (missing, empty, malformed, or
non-HTTP(S)) is invalid: it never triggers an HTTP request and appears in
\code{map} with \code{fetch_outcome = "input_invalid"}, no \code{source_id}, and
invalid-URL error metadata.

The request policy is deterministic and intentionally conservative: HTTP
\code{GET}; TLS verified with platform defaults; a total timeout per origin
including redirects; at most five redirects; redirects allowed only to
\code{http}/\code{https}; HTTP-to-HTTPS upgrades allowed but every HTTPS-to-HTTP
downgrade rejected as \code{redirect_error}; no URL userinfo, authorization,
cookies, or caller session state forwarded; a package fetch user agent
(\verb{robotstxtr/<version>}) unless the caller supplies one; and no automatic
retries. Each response is classified into a stable \code{fetch_outcome}. For a
\code{fetched} outcome the exact decoded entity bytes are stored once as a raw
vector in the source row's \code{body} (an empty body is valid); all other
outcomes store no body.

\code{max_bytes} caps the decoded response body. The limit is enforced on decoded
entity bytes (after any HTTP content decoding, such as gzip) by a streaming
read that stops the moment the decoded byte count exceeds the limit; the
compressed entity is never fully downloaded before the check. A body over the
limit ends as \code{body_too_large} with no stored body (never a truncated body);
a body at or under the limit is stored normally as a \code{fetched} result.
}
\examples{
# robots_fetch() performs live HTTP. To keep this example offline and
# deterministic, the transport is mocked with httr2 so every request returns
# the same robots.txt; a real call needs no such wrapper.
httr2::with_mocked_responses(
  function(req) {
    httr2::response(
      status_code = 200L, url = req$url,
      body = charToRaw("user-agent: *\nDisallow: /private\n")
    )
  },
  {
    # Both URLs share one fetch-origin, so /robots.txt is fetched once.
    robots_fetch(c("https://example.com/", "https://example.com/private"))
  }
)

}
\seealso{
\code{\link[=allowed_by_robots_url]{allowed_by_robots_url()}} to fetch and match in one step.
}
