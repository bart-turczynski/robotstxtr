% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine-contract-v1.R
\name{robots_evaluate_url_v1}
\alias{robots_evaluate_url_v1}
\title{Fetch and evaluate robots.txt under an explicit engine profile}
\usage{
robots_evaluate_url_v1(
  url,
  robots_product_token,
  robots_policy_ruleset,
  matcher_backend,
  timeout = 10,
  max_bytes = 524288L,
  fetch_user_agent = NULL,
  ssrf_guard = TRUE
)
}
\arguments{
\item{url}{A character vector of URLs to evaluate.}

\item{robots_product_token}{A character vector of length one or
\code{length(url)} used only for robots user-agent group selection. The Google
backend accepts any valid Google robots product token and yields Google
semantics for it, not a prediction of the named crawler.}

\item{robots_policy_ruleset}{An explicit ruleset, length one or
\code{length(url)}: \code{"google"}, \code{"yandex"}, \code{"rfc9309"}, \code{"bing"}, or
\code{"assumed_rfc9309"}.}

\item{matcher_backend}{An explicit matcher backend, length one or
\code{length(url)}: \code{"google"}, \code{"yandex"}, \code{"rfc9309"}, or \code{"bing"}.}

\item{timeout}{A single positive, finite numeric total timeout in seconds per
origin, including redirects. Defaults to \code{10}.}

\item{max_bytes}{A single positive, finite, whole-number decoded-body byte
limit no greater than \code{.Machine$integer.max}, coerced to integer and
recorded on each source row. A whole-number double (for example \code{524288})
is accepted; fractional, non-positive, out-of-range, or non-scalar values
are call-level errors. Defaults to \code{524288L}.}

\item{fetch_user_agent}{\code{NULL} (use the package fetch user agent) or a single
non-empty character HTTP user agent to send instead.}

\item{ssrf_guard}{A single logical. When \code{TRUE} (the default) a structural
SSRF guard refuses, before any request, both the initial origin and every
redirect target that resolves to a private, loopback, link-local, or
cloud-metadata address (outcome \code{ssrf_blocked}, no socket opened). Set to
\code{FALSE} to deliberately opt out — for example to fetch \code{robots.txt} from a
trusted intranet host. Opting out removes that protection; only disable it
for hosts you control and trust.}
}
\value{
A \code{robots_engine_decisions_v1} object with \code{results}, neutral
\code{evidence}, and \code{contract} components.
}
\description{
Versioned URL-first entry point for the engine-aware v1 contract. It records
neutral acquisition and safety evidence before applying the explicitly
selected policy ruleset and matcher backend. \code{fetch_user_agent} is the HTTP
request User-Agent and is never used as \code{robots_product_token}.
}
\details{
As with \code{\link[=robots_evaluate_text_v1]{robots_evaluate_text_v1()}}, the \code{robots_product_token} drives ONLY
user-agent group selection. Each matcher backend is authoritative only for
its own vendor semantics; the Google backend accepts any valid Google robots
product token and returns Google semantics for it, which is not a claim of
compatibility with the named crawler, while the Yandex and Bing backends are
bounded to their supported vendor profiles and RFC 9309 behavior is available
only under the \code{rfc9309} backend. See
\code{robots_engine_contract_v1()$matcher_capability}.
}
