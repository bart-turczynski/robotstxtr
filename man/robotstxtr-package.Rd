% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robotstxtr-package.R
\docType{package}
\name{robotstxtr-package}
\alias{robotstxtr}
\alias{robotstxtr-package}
\title{robotstxtr: Engine-Aware 'robots.txt' Policy with Google's Matcher Algorithm}
\description{
Evaluates whether URLs may be crawled according to 'robots.txt' rules. A versioned engine-aware facade keeps neutral fetch evidence, per-engine status policy, robots product tokens, and matcher capabilities separate, returning explicit non-decisions when policy is undocumented or a matcher is unavailable. The available matcher is a vendored, upstream-test-validated snapshot of Google's open-source C++ 'robots.txt' matcher, preserving Googlebot behavior. Fetches use a deterministic, conservative policy built on 'httr2', with fetch origins constructed by 'rurl'. All network access in tests, examples, and vignettes is mocked or served locally, so no internet connection is required to install, test, or check the package. The bundled matcher derives from Google's 'robotstxt' project \url{https://github.com/google/robotstxt}, distributed under the Apache License 2.0; see the package NOTICE.
}
\examples{
# A short offline tour; no call below performs a network request.

# 1. Match URLs against a supplied robots.txt body.
decisions <- allowed_by_robots_text(
  "user-agent: *\ndisallow: /private",
  c("https://example.com/page", "https://example.com/private"),
  "my-bot"
)
decisions$results[, c("url", "allowed", "matched_rule_value")]

# 2. Inspect the stored body those decisions came from. The default
# `n = 20` prints a short preview, so ask for the whole body here.
robots_body(decisions, n = Inf)

# 3. The same match through the engine-aware facade, which names the
# policy ruleset and the matcher backend explicitly.
engine <- robots_evaluate_text_v1(
  "user-agent: *\ndisallow: /private",
  "https://example.com/private",
  robots_product_token = "my-bot",
  robots_policy_ruleset = "google",
  matcher_backend = "google"
)
engine$results$url_decision

# 4. Report parse-level problems in a document.
robots_validate_text("disallow: /before-agent\n")$diagnostics

# 5. Inspect the contract: identifiers, value sets, backend capabilities.
contract <- robots_engine_contract_v2()
contract$contract_id
contract$matcher_backends
}
\seealso{
Engine-aware evaluation (versioned facade): \code{\link[=robots_evaluate_text_v1]{robots_evaluate_text_v1()}},
\code{\link[=robots_evaluate_url_v1]{robots_evaluate_url_v1()}}.
Contract introspection: \code{\link[=robots_engine_contract_v1]{robots_engine_contract_v1()}},
\code{\link[=robots_engine_contract_v2]{robots_engine_contract_v2()}}.
Google-matcher entry points: \code{\link[=allowed_by_robots_text]{allowed_by_robots_text()}},
\code{\link[=allowed_by_robots_url]{allowed_by_robots_url()}}.
Acquisition evidence: \code{\link[=robots_fetch]{robots_fetch()}}.
Document validation: \code{\link[=robots_validate_text]{robots_validate_text()}}, \code{\link[=robots_validate_url]{robots_validate_url()}}.
Result inspection: \code{\link[=robots_body]{robots_body()}}, \code{\link[=as_legacy_robots_decisions_v1]{as_legacy_robots_decisions_v1()}}.

The bundled matcher is a vendored, upstream-test-validated snapshot of
Google's open-source C++ robots.txt matcher. Fetch origins are constructed
with the \pkg{rurl} package and HTTP is performed by \pkg{httr2}.
}
\author{
\strong{Maintainer}: Bart Turczynski \email{bartek@turczynski.pl} (\href{https://orcid.org/0000-0002-8788-7980}{ORCID})

Authors:
\itemize{
  \item Bart Turczynski \email{bartek@turczynski.pl} (\href{https://orcid.org/0000-0002-8788-7980}{ORCID})
}

}
\keyword{internal}
